{
  "name": "pass_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "rebuild": "electron-rebuild -f -w sqlite3",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "electron-packager . pass_app --overwrite --asar=true --platform=win32 --arch=ia32 --icon=/public/icons/repair.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Electron Tutorial App\""
  },
  "author": "entropy",
  "license": "ISC",
  "dependencies": {
    "electron": "^1.7.9",
    "knex": "^0.14.0",
    "node-fetch": "^1.7.3",
    "sqlite3": "^3.1.13",
    "uuid": "^3.1.0"
  },
  "devDependencies": {
    "electron-packager": "^9.1.0",
    "electron-rebuild": "^1.6.0"
  }
}
